# Copy to .env.ios.local and fill in. Used by scripts/ios-device-install.sh
# (npm run ios:device). Both values are optional: the team id falls back to the
# OU of your "Apple Development" certificate, and the device to the only paired
# one. Set them when the fallback is ambiguous.
IOS_DEVELOPMENT_TEAM=
IOS_DEVICE_ID=
